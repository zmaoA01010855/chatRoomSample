export const AuthReducer = (state, action) => {
    switch (action.type) {
        case "Login":
            return state;
        case "Sign In":
            return state;
        default:
            return state;
    }
}